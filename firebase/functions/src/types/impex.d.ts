export interface ExportRequest {
    eventId?: string,
    eventData?: boolean,
    screens?: boolean,
    liveboard?: boolean,
    desk?: boolean,
    program?: boolean,
    feed?: boolean,
    gameboard?: boolean,
}

